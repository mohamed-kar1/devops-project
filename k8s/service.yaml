apiVersion: v1
kind: Service
metadata:
  name: devops-api-service
  namespace: default
spec:
  selector:
    app: devops-api
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080  # Accessible via http://MASTER_IP:30080
